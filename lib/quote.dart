class Quote {
  String text;
  String author;

  // Put curly braces, it means its named parameters
  Quote({required this.text, required this.author});
}

// Named Parameters
// Quote myquote = Quote(text: "Quote Oscar", author: "Oscar Wilde");
